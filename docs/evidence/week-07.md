# Week 7 — Object-oriented parcel model

Target: represent a parcel as an object with encapsulated state and a receipt behaviour.

Evidence: a `Parcel` object prints its ID, recipient and size as one receipt.
