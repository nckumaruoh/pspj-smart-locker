# Week 6 — Methods and validation

Target: use methods to validate parcel identifiers and sizes before intake.

Evidence: `PKG-1024` with `MEDIUM` is accepted; malformed IDs and unsupported sizes are rejected.
