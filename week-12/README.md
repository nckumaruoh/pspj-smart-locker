# Week 12 - Final Release

The final integration combines validated intake, size-based allocation, collection, a sorted parcel register, and file persistence.

## Final acceptance checks

- [x] Three compatible parcels receive suitable lockers.
- [x] A collected parcel changes from READY to COLLECTED.
- [x] The register is displayed in parcel-ID order.
- [x] The final state is persisted in `parcel-register.txt`.
